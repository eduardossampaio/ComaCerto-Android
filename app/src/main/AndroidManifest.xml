<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="apps.esampaio.com.comacerto">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        tools:replace="android:theme"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ComaCertoAppTheme">
        <activity
            android:name=".view.SplashActivity"
            android:theme="@style/ComaCertoAppTheme_NoActionBar">

        </activity>
        <activity
            android:theme="@style/ComaCertoAppTheme_NoActionBar"
            android:name=".view.HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.meals.register.AddNewMealActivity">

        </activity>

        <activity
            android:name=".view.meals.edit.EditMealActivity">
        </activity>

    </application>

</manifest>